# repo-onboard v1.0.3 — change specification

Apply these changes to the plugin files. Each change includes
the exact file, what to change, and why. Apply all changes,
then commit as a single version bump.

These changes come from the first real test of the plugin
(Anki flashcard converter project). Every change addresses a
real problem discovered during testing.

Read the existing files before making changes. Preserve
everything that isn't explicitly modified below.

---

## Change 1: Add Phase 0 to requirements interview

**File:** `skills/project-onboard/references/requirements-phase.md`

**Where:** Add a new section BEFORE "## Phase 1: Who and why"

**Add:**

```markdown
## Phase 0: Know the person (before the interview begins)

Before asking about the project, understand who you're talking to.
This calibrates the entire session.

Ask:
- "Before we dive in — tell me briefly about your technical
  background. What languages or tools are you comfortable with?"
- "Have you built something like this before, or is this new
  territory for you?"

How this affects the interview:
- If experienced: ask open questions first, offer options only
  when they're stuck
- If learning: lead with 2-3 concrete options for each question,
  explain tradeoffs, give more guidance
- Calibrate confidence scores based on their experience level
  (see confidence calibration rules)

Do NOT spend more than 2 questions on this. It's a warm-up,
not a phase.
```

---

## Change 2: Shift design interview to guided exploration

**File:** `skills/project-onboard/references/design-phase.md`

**Where:** Replace the introduction section (keep the 3 core
questions and tech stack section, but change the approach)

**Replace introduction with:**

```markdown
# Design interview — phase reference

You are conducting the technical design phase. Read docs/requirements.md
first — every question should reference specific decisions from the
requirements. Do NOT re-interview requirements.

## Approach: guided exploration, not interrogation

The user came to this tool because they have an idea but don't know
HOW to build it. Asking "describe the data transformations" to someone
who doesn't know what transformations are possible is unhelpful.

For EVERY design question, follow this pattern:
1. Present 2-3 concrete approaches with tradeoffs explained
2. Give the user space: "Take a moment. Is there anything about
   your project that would affect this choice — something I
   haven't asked about?"
3. Wait for their response before moving on
4. If they defer ("go with your suggestion"), push back once:
   "Tell me in your own words why this fits your project."
5. If they still defer, accept but mark confidence as medium

Adjust the level of explanation based on Phase 0:
- Experienced user: brief options, assume they understand tradeoffs
- Learning user: explain each option in plain language, recommend
  one as the default, explain why
```

**Also add after each question's options section:**

```markdown
After presenting options, always add:
"Take a moment to think about this. And is there anything about
your project that would affect this choice — something I haven't
asked about?"

Wait for their full response. Do not rush to the next question.
```

---

## Change 3: Add passive agreement pushback rule

**Files:** BOTH:
- `skills/project-onboard/references/requirements-phase.md`
- `skills/project-onboard/references/design-phase.md`

**Where:** Add to the rules section of each file

**Add:**

```markdown
## Passive agreement rule

When the user defers to your suggestion without engaging
("go with it", "sounds good", "yeah what you said", "it's fine"),
push back ONCE:

"I want to make sure this is YOUR decision. In your own words —
why does this approach fit your project?"

If they defer again after one pushback, accept it and note:
(confidence: medium — user deferred to suggested approach)

Do NOT push back more than once — respect their choice.
```

---

## Change 4: Fix confidence calibration

**Files:** BOTH:
- `skills/project-onboard/references/requirements-phase.md`
- `skills/project-onboard/references/design-phase.md`

**Where:** Replace or update the existing confidence scoring
section in each file

**Replace with:**

```markdown
## Confidence calibration

Base confidence on BOTH answer clarity AND user experience
(from Phase 0):

- Experienced user + clear answer = high
- Experienced user + vague answer = medium
- Learning user + clear answer = medium
  (they understood the question but may not understand
  the implications)
- Learning user + vague answer = low
- User deferred to suggestion = medium (regardless of
  experience level)

If the user said in Phase 0 that this is new territory,
default to medium confidence. Only upgrade to high when they
demonstrate specific domain knowledge in their answers.

Never mark ALL sections as "high" — every project has
uncertainty somewhere. If everything looks high, you're
not being honest about what you don't know.
```

---

## Change 5: Add context: fork to pipeline and revise skills

**Files:** Update the YAML frontmatter in ALL of these:
- `skills/project-onboard/SKILL.md`
- `skills/revise-requirements/SKILL.md`
- `skills/revise-design/SKILL.md`
- `skills/revise-tasks/SKILL.md`

**What:** Add `context: fork` to the frontmatter of each file,
alongside the existing fields.

Do NOT add `context: fork` to the supporting skills
(explore-system, investigate-bug, stakeholder-brief, write-rfc).
They need conversation context during development.

---

## Change 6: Add disable-model-invocation: true to ALL skills

**Files:** Update the YAML frontmatter in ALL skill SKILL.md files:
- `skills/project-onboard/SKILL.md` (already has it — verify)
- `skills/revise-requirements/SKILL.md` (already has it — verify)
- `skills/revise-design/SKILL.md` (already has it — verify)
- `skills/revise-tasks/SKILL.md` (already has it — verify)
- `skills/explore-system/SKILL.md` (ADD this)
- `skills/investigate-bug/SKILL.md` (ADD this)
- `skills/stakeholder-brief/SKILL.md` (ADD this)
- `skills/write-rfc/SKILL.md` (ADD this)

**Why:** No skill should auto-trigger during development. The
user is always in control. They invoke skills explicitly when
they need them. This prevents the onboarding interview from
firing on "I have a project idea" and prevents supporting
skills from activating unexpectedly during normal coding.

---

## Change 7: Review phase catches implicit decisions

**File:** `skills/project-onboard/references/review-phase.md`

**Where:** Add to the "## What to check" section

**Add:**

```markdown
### Implicit decisions
- Are there format or structure details assumed but not stated?
  (file encoding, header rows, delimiters, line endings, config
  file format, default values)
- Are there decisions that will need to be made during
  implementation but weren't discussed?
- Does the output format have ambiguities that could cause
  problems? (e.g., CSV with or without headers, JSON pretty
  printed or minified)
```

---

## Change 8: Update pipeline completion message

**File:** `skills/project-onboard/SKILL.md`

**Where:** The "### Done" section at the end

**Replace with:**

```markdown
### Done

Tell the user:
"Your project foundation is ready:
- docs/requirements.md — what you're building and why
- docs/design.md — how you're building it
- docs/tasks.md — the order to build it
- CLAUDE.md — Claude Code configuration
- .claude/skills/ — supporting tools (invoke with / when needed)

Your next step: use Claude Code's plan mode. The plan will be
dramatically better because it has structured specs to work from.

If your requirements or design change later, use:
/repo-onboard:revise-requirements
/repo-onboard:revise-design
/repo-onboard:revise-tasks"
```

---

## Change 9: Update README

**File:** `README.md`

**Changes:**

1. Update the one-liner description to:
"repo-onboard turns your messy idea into structured specs.
Claude Code's plan mode does the rest — and it does it 10x
better with specs than without."

2. Fix the install command syntax:
```
/plugin marketplace add dannyavrs/repo-onboard
/plugin install repo-onboard@dannyavrs
```

3. Add or update the scope boundary section:
"repo-onboard is a project initiation tool. It produces specs
and configuration. It does NOT generate code, analyze existing
repos, enforce specs during development, or auto-update docs
when code changes. It gives you the best possible starting
point. What you build from there is up to you."

4. Note that all skills are manually invoked:
"All skills use disable-model-invocation — they never auto-trigger.
You invoke them explicitly when you need them."

---

## Change 10: Bump version in plugin.json

**File:** `.claude-plugin/plugin.json`

**Change:** Update version to "1.0.3"

---

## After applying all changes

1. Read through every modified file to verify consistency
2. Make sure no duplicate sections were created
3. Verify all YAML frontmatter is valid (no tabs, proper indentation)
4. Commit: `git commit -am "feat: v1.0.3 — interview quality + architecture fixes from first real test"`
5. Push to GitHub

## Recommended testing

```bash
cd /path/to/empty-test-folder
claude --plugin-dir /path/to/repo-onboard
```

Then type: `/repo-onboard:project-onboard`

Verify:
- Phase 0 warm-up questions appear first
- Design questions offer options, not open interrogation
- Pauses appear after each option set
- Passive agreement gets pushed back once
- Confidence scores are NOT all "high"
- Pipeline runs in forked context
- Completion message mentions plan mode
